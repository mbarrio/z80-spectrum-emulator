int	FileDlg(	int nIndex,		// index of file filter to use
					LPSTR szPathName,	// returns the path and filename
					BOOL bLoad,			// TRUE=Open file;  FALSE = Save File
					HWND hParentWnd);	// handle of the parent window

